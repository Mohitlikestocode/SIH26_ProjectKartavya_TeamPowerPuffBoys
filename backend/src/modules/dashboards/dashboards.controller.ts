import type { Request, Response, NextFunction } from "express";
import * as service from "./dashboards.service";

// Dashboards controllers — thin HTTP layer over dashboards.service.ts.
// TODO: implement handlers as each phase lands.
