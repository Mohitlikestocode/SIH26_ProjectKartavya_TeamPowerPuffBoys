import { Router } from "express";
import * as controller from "./multilingual.controller";

// Multilingual routes — Phase scaffold, endpoints implemented per prompt.md build order.
export const multilingualRouter = Router();

// TODO: wire actual endpoints for the Multilingual module.
