import type { Request, Response, NextFunction } from "express";
import * as service from "./competency.service";

// Competency controllers — thin HTTP layer over competency.service.ts.
// TODO: implement handlers as each phase lands.
