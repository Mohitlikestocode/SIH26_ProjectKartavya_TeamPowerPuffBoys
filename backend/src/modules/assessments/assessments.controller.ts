import type { Request, Response, NextFunction } from "express";
import * as service from "./assessments.service";

// Assessments controllers — thin HTTP layer over assessments.service.ts.
// TODO: implement handlers as each phase lands.
