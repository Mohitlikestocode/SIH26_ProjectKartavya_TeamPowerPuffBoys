import { Router } from "express";
import * as controller from "./sessions.controller";

// Sessions/QR routes — Phase scaffold, endpoints implemented per prompt.md build order.
export const sessionsRouter = Router();

// TODO: wire actual endpoints for the Sessions/QR module.
