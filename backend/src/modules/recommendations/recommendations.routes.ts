import { Router } from "express";
import * as controller from "./recommendations.controller";

// Recommendations routes — Phase scaffold, endpoints implemented per prompt.md build order.
export const recommendationsRouter = Router();

// TODO: wire actual endpoints for the Recommendations module.
